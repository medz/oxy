import '../dirver.dart';
import '../request.dart';

class DefaultAdapter implements Dirver {
  @override
  Future request(Request request) {
    // TODO: implement request
    throw UnimplementedError();
  }
}
