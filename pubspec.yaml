name: oxy
description: A production-ready HTTP client for Dart and Flutter with middleware, retries, timeout, and safe APIs.
version: 0.1.0
repository: https://github.com/medz/oxy

topics:
  - http
  - flutter
  - networking
  - middleware
  - api-client

funding:
  - https://github.com/sponsors/medz
  - https://opencollective.com/openodroe

environment:
  sdk: ^3.8.0

dependencies:
  ht: ^0.1.0
  ocookie: ^0.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.2
