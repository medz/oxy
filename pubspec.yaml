name: oxy
description: -> A better HTTP client
version: 0.0.0-dev.0
repository: https://github.com/medz/oxy

environment:
  sdk: ^3.8.0

dependencies:
  meta: ^1.17.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.2
