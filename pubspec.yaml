name: oxy
description: A modern, feature-rich HTTP client for Dart with Web API compatibility, supporting fetch-like syntax, request/response handling, and cross-platform adapters.
version: 0.0.1
repository: https://github.com/medz/oxy

environment:
  sdk: ^3.8.0

dependencies:
  meta: ^1.17.0
  mime: ^2.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.2
